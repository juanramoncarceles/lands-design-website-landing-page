☐ In the smaller than 700px version the try it button and the two leafs should have a clear focus and some hover.
☐ The videos can receive focus, make it clear and actionalble through Enter.
☐ Remove the compressed options so the built output is by default not compressed.